2.3 
i) DESC EMP_2018;DESC PRODUCT_MASTER_2018;DESC CLIENT_MASTER_2018;DESC SALESMAN_MASTER_2018;

ii) SELECT EMPN,JOB FROM EMP_2018;

iii) SELECT *FROM EMP_2018; 

iv) SELECT EMPN,SAL,SAL*1.5 AS "NEWSAL" FROM EMP_2018;

v) SELECT * FROM EMP_2018 WHERE JOB='CLERK';

vi) SELECT DISTINCT DEPTNO FROM EMP_2018;

vii) SELECT ENAME,SAL FROM EMP_2018 WHERE SAL > 1000 AND SAL <= 1500;

viii) SELECT ENAME,SAL FROM EMP_2018 WHERE SAL = 3000 OR SAL = 5000;

ix) SELECT ENAME FROM EMP_2018 WHERE ENAME LIKE '_A%';

x) SELECT * FROM EMP_2018 WHERE SAL > 1500 AND DEPTNO = 10 OR DEPTNO = 20;

xi) SELECT LOWER(ENAME),INITCAP (ENAME),LOWER(JOB), INITCAP(JOB), LENGTH(ENAME) FROM EMP_2018 ORDER BY SAL DESC;

3. 
i) SELECT SYSDATE FROM DUAL;

ii) SELECT * FROM DUAL;

iii) select 15+10-5*5/5  from dual;

iv) select * from product_master_2018 where sell_price-cost_price > 25 ;

v) select ename|| ',' || job  as "ename job" from emp_2018;

vi) select * from emp_2018 order by job asc;

vii) select rpad(empn,5,'*') as Empno,lpad(sal,5,'$') as Salary from emp_2018 where ename like '%AM%';

4.
i) select lower(ename) as ENAME from emp_2018;

ii) update product_master_2018 set qty_on_hand = 15 where qty_on_hand < 10;

iii) update salesman_master_2018 set salesman_name='SATISH',sal_amt=2000 where salesman_name='MANISH';

iv) COMMIT;

v) update product_master_2018 set qty_on_hand = 20 where qty_on_hand < 10;

savepoint s1;

INSERT INTO PRODUCT_MASTER_2018 (PRODUCT_NO,DESCRIPTION, PROFIT_PERCENT,UNIT_MEASURE,QTY_ON_HAND,RECORD_LVL,SELL_PRICE,COST_PRICE) 
VALUES('&PRODUCT_NUMBER', '&DESCRIPTION', &PROFIT_PERCENT,'&UNIT_MEASURE',&QTY_ON_HAND,&RECORD_LVL,&SELL_PRICE,&COST_PRICE);

SELECT * FROM PRODUCT_MASTER_2018;

ROLLBACK TO savepoint s1;

SELECT * FROM PRODUCT_MASTER_2018;

vi) DELETE FROM EMP_2018 WHERE (SYSDATE-HIREDATE)/365 <35;

vii) UPDATE EMP_2018 SET SAL = (SAL+1000) , COMM = COMM+(COMM*.1);

viii) Delete from emp_2018 where empn < 7500 and sal between 1500 and 1800;

5.
i) SELECT SQRT(15) "SQUARE ROOT",ABS(-30) "ABSOLUTE VALUE",FLOOR(35.625) "FLOOR VALUE",CEIL(35.625) "CEILING VALUE",ROUND(35.628,2) "ROUND",TRUNC(35.628,2) "TRUNCATE" FROM DUAL;

ii) SELECT EMPN,MONTHS_BETWEEN(SYSDATE,HIREDATE) "MONTHS_WORKED",NEXT_DAY(ADD_MONTHS(HIREDATE,6),'MONDAY') "SAL_REV" FROM EMP_2018;

iii) SELECT (SAL+COMM)*12 + NVL(COMM,0) "ANNUAL_SAL" FROM EMP_2018;

iv) SELECT SUM(SAL) "TOTAL SALARY",AVG(SAL) "AVERAGE SALARY", MIN(SAL) "MINIMUM SALARY",MAX(SAL) "MAXIMUM SALARY" FROM EMP_2018;

v) SELECT SUM(SAL) "TOTAL SALARY" FROM EMP_2018 GROUP BY DEPTNO;

vi) SELECT DEPTNO,SAL "SALARY",HIREDATE FROM EMP_2018 WHERE SAL IN(SELECT MIN(SAL) FROM EMP_2018 GROUP BY DEPTNO HAVING DEPTNO <>30 AND MIN(SAL)<>800) ORDER BY HIREDATE DESC;

vii) SELECT MIN(AVG(SAL)) "MINIMUM AVERAGE SALARY" FROM EMP_2018 GROUP BY DEPTNO;

viii) SELECT EMPN,TO_CHAR(HIREDATE,'"The" Ddspth "of" Month,YYYY') "HIREDATE" FROM EMP_2018 WHERE HIREDATE=TO_DATE('DECEMBER 03,1981','MONTH DD,YYYY');

6.
i) SELECT * FROM SALESMAN_MASTER_2018 WHERE SALESMAN_NAME LIKE 'K%';

ii) SELECT PRODUCT_NO,DESCRIPTION,PROFIT_PERCENT,SELL_PRICE FROM PRODUCT_MASTER_2018 WHERE PROFIT_PERCENT BETWEEN 3 AND 6;

iii) SELECT NAME FROM CLIENT_MASTER_2018;

iv) SELECT * FROM CLIENT_MASTER_2018 WHERE CITY='BOMBAY';

v) SELECT SALESMAN_NAME FROM SALESMAN_MASTER_2018 WHERE SAL_AMT=3000;

vi) UPDATE CLIENT_MASTER_2018 SET CITY='BOMBAY' WHERE CLIENT_NO = 'C00005';

vii) UPDATE CLIENT_MASTER_2018 SET BAL_DUE=1000 WHERE CLIENT_NO ='C00001';

viii) UPDATE PRODUCT_MASTER_2018 SET COST_PRICE=950.00 WHERE DESCRIPTION='1.22 FLOPPIES';

ix) UPDATE SALESMAN_MASTER_2018 SET CITY='MUMBAI' WHERE SALESMAN_NAME LIKE 'R%';

x) SELECT NVL(COUNT(STATE),NULL) TOTAL_NO_OF_EMPLOYEES,STATE FROM CLIENT_MASTER_2018 GROUP BY STATE;

xi) SELECT ENAME FROM EMP_2018 WHERE HIREDATE LIKE '%DEC%';

xii) SELECT ENAME FROM EMP_2018 WHERE TO_CHAR(HIREDATE,'MON')= TO_CHAR(SYSDATE,'MON');

7.
i) SELECT ORDER_NO,CLIENT_NO,TO_CHAR(ORDER_DATE,'DD/MM/YY') ORDER_DATE FROM SALES_ORDER_2019 ORDER BY ORDER_DATE;

ii) SELECT * FROM SALES_ORDER_2019 WHERE ORDER_DATE <= TO_DATE('18-JUNE -1997');

iii) SELECT ORDER_NO,PRICE FROM SALES_ORDER_2019 WHERE TO_NUMBER(PRICE)=20000;

8.
i) SELECT EMP_NO,NAME,PAY_RATE FROM EMPLOYEE WHERE EMP_NO <123460 AND PAY_RATE >ANY(SELECT PAY_RATE FROM EMPLOYEE WHERE EMP_NO>=123460);

ii) SELECT NAME FROM EMPLOYEE WHERE EMP_NO IN(SELECT EMP_NO FROM DUTY_ALLOCATION WHERE POSITIONING_NO IN(SELECT POSITIONING_NO FROM POSITION WHERE SKILL='CHEF'));

iii) SELECT NAME FROM EMPLOYEE WHERE PAY_RATE=(SELECT MIN(PAY_RATE) FROM EMPLOYEE);

iv) SELECT NAME FROM EMPLOYEE WHERE PAY_RATE=(SELECT MIN(PAY_RATE) FROM EMPLOYEE WHERE SKILL='CHEF');

v) SELECT NAME,PAY_RATE FROM EMPLOYEE WHERE EMP_NO IN(SELECT EMP_NO FROM DUTY_ALLOCATION);

vi) SELECT EMP_NO FROM EMPLOYEE WHERE SKILL='WAITER' UNION SELECT EMP_NO FROM DUTY_ALLOCATION WHERE POSITIONING_NO=321;

vii) SELECT NAME FROM EMPLOYEE WHERE EMP_NO =(SELECT EMP_NO FROM EMPLOYEE WHERE SKILL='CHEF' INTERSECT SELECT EMP_NO FROM DUTY_ALLOCATION);

viii) SELECT EMP_NO FROM DUTY_ALLOCATION  GROUP BY EMP_NO HAVING COUNT(*)>=2;

ix) SELECT DEPTNO,MIN(SAL) FROM EMP_2018 GROUP BY DEPTNO HAVING MIN(SAL)>(SELECT MIN(SAL) FROM EMP_2018 WHERE DEPTNO=20);

x) SELECT ENAME,SAL,DEPTNO FROM EMP_2018 WHERE DEPTNO IN(SELECT DEPTNO FROM EMP_2018 WHERE COMM>0) AND SAL IN(select sal from emp_2018 where comm>0);

xi) SELECT EMPN,ENAME FROM EMP_2018 WHERE EMPN NOT IN(SELECT MGR FROM EMP_2018);

xii) SELECT ENAME,DEPTNO,SAL FROM EMP_2018 WHERE SAL =ANY(SELECT e.SAL  FROM EMP_2018 e,SCOTT.DEPT d WHERE e.DEPTNO=d.DEPTNO AND d.LOC='DALLAS') AND COMM=ANY(SELECT e.COMM  FROM EMP_2018 e,SCOTT.DEPT d WHERE e.DEPTNO=d.DEPTNO AND d.LOC = 'DALLAS' AND e.COMM <>0);
checking 

xiii) SELECT ENAME,HIREDATE,SAL FROM EMP_2018 WHERE SAL =ALL(SELECT SAL FROM EMP_2018 WHERE ENAME='SCOTT') AND COMM=ALL(SELECT COMM FROM EMP_2018 WHERE ENAME='SCOTT');

xiv) SELECT * FROM EMP_2018 WHERE SAL >ALL(SELECT SAL FROM EMP_2018 WHERE JOB='CLERK') ORDER BY SAL DESC;

xv) SELECT ENAME,DEPTNO,SAL FROM EMP_2018 WHERE EMPN IN(SELECT EMPN FROM EMP_2018 WHERE SAL IN(SELECT MAX(SAL) FROM EMP_2018 WHERE SAL<(SELECT MAX(SAL) FROM EMP_2018) GROUP BY DEPTNO));

xvi) SELECT * FROM(SELECT * FROM EMP_2018 ORDER BY hiredate) WHERE ROWNUM<=4 ORDER BY hiredate;

xvii) SELECT ENAME NAMES,SAL SALARIES FROM(SELECT ENAME,SAL FROM EMP_2018 ORDER BY SAL DESC) WHERE ROWNUM<=3 ORDER BY SAL DESC;

9.
i) CREATE TABLE EMPLOYEE_NEW (EID,ENAME,ESKILL,ERATE) AS SELECT EMP_NO,NAME,SKILL,PAY_RATE FROM EMPLOYEE WHERE 1=2;

ii) ALTER TABLE EMPLOYEE_NEW ADD HIREDATE DATE;

iii) ALTER TABLE EMPLOYEE_NEW MODIFY ENAME VARCHAR2(50);

iv) ALTER TABLE EMPLOYEE_NEW DROP COLUMN ESKILL;

DESC EMPLOYEE_NEW;

v) ALTER TABLE EMPLOYEE_NEW MODIFY HIREDATE VARCHAR2(20);

DESC EMPLOYEE_NEW;

vi) ALTER TABLE EMPLOYEE_NEW MODIFY EID VARCHAR2(10);

vii) ALTER TABLE EMPLOYEE_NEW RENAME COLUMN ENAME TO NAME;

viii) ALTER TABLE EMPLOYEE_NEW RENAME TO ENEW;

ix) COMMENT ON TABLE ENEW IS 'THIS IS EMPLOYEE TABLE';

SELECT COMMENTS FROM USER_TAB_COMMENTS WHERE TABLE_NAME='ENEW';

x) ALTER TABLE ENEW DROP CONSTRAINT ENEW_PK CASCADE; //CHECKING 

xi) ALTER TABLE ENEW ADD CONSTRAINT emp_eid_pk PRIMARY KEY(EID);

DESC ENEW;

DROP PRIMARY KEY:=>  ALTER TABLE ENEW DROP CONSTRAINT emp_eid_pk CASCADE;

DESC ENEW;

xii) CREATE TABLE EMPLOYEE_NEW AS (SELECT EMP_NO AS EID,NAME AS ENAME,SKILL AS ESKILL,PAY_RATE AS ERATE FROM  EMPLOYEE);

DELETE EMPLOYEE_NEW;

ROLLBACK;

SELECT * FROM EMPLOYEE_NEW;

xiii) TRUNCATE TABLE ENEW;

SELECT * FROM ENEW;

INSERT INTO ENEW VALUES ('123456','PAPAI',4,'12-JUN-2012');

SELECT * FROM ENEW;

xiv) DELETE FROM USER_TAB_COMMENTS WHERE TABLE_NAME='ENEW';

xv) DROP TABLE ENEW;

xvi) CREATE TABLE EMP AS SELECT * FROM EMPLOYEE WHERE 1=2;

DESC EMP;

10.
i) create view EMP_VIEW as (select empno as EMP_ID,ename as FIRST_NAME,deptno as DEPT_ID from EMP_2018);

ii) create or replace view EMP_VIEW(EID,FNAME,DID) as (select empno,ename,deptno from EMP_2018);

iii) SELECT VIEW_NAME,TEXT FROM USER_VIEWS WHERE VIEW_NAME LIKE 'EMP_VIEW';

iv) select FNAME,DID from EMP_VIEW;

v) create view dept2 as (select EID,FNAME from EMP_VIEW) with read only;

vi) create view EMP_DEPT_VIEW(dept_name,min_salary,max_salary,average_sal) as (select (select d.dname from scott.dept d where e.deptno=d.deptno),min(sal),max(sal),avg(sal) from emp_2018 e group by deptno);

vii) CREATE SEQUENCE EMPLOYEE_EMPNO_SEQ START WITH 60 MINVALUE 60 MAXVALUE 200 INCREMENT BY 10;

viii) select sequence_name,max_value,min_value from user_sequences where sequence_name like 'EMPLOYEE_EMPNO_SEQ';

ix) create index employee_index on employee(SKILL);

x) SELECT INDEX_NAME,UNIQUENESS FROM USER_INDEXES WHERE TABLE_NAME LIKE 'EMPLOYEE';

xii) CREATE SYNONYM POS1_SYN FOR POSITION1;

SELECT * FROM  POS1_SYN;

DROP TABLE POSITION1;

xiii) DROP SEQUENCE EMPLOYEE_EMPNO_SEQ;

DROP INDEX employee_index;

DROP SYNONYM POS1_SYN;
 
